# gogoke Agent rules

## Scope Discipline

优先采用完整满足任务的最小连贯改动；不得为方便、清理、重构、格式化或推测性改进而扩大范围。只有目标的真实依赖要求时才扩大范围，并保持在必要的最小边界内。

`running` 状态不等于进展；等待必须指向可验证的命令、状态变化或外部对象。没有活跃命令、具名 diff、具体结论或明确可观察的等待对象时，及时核对并交还控制权。

## 构建、发布与平台约束

Owner 的机器开着强制模式的智能应用控制，会不可预测地拦截本机新编译的未签名程序（Win32 4551，Code Integrity 3077/3033），且不支持任何例外。完整理由、来源与反例见 [docs/governance/gogoke-build-and-release.md](docs/governance/gogoke-build-and-release.md)；涉及构建、测试、发布、更新或新增可执行文件的计划必须先引用它。

- gogoke 的原生代码（Rust、C 及一切产出 exe/dll 的东西）一律在云端 CI 编译和测试。新增 crate、可执行文件或原生测试，必须同时接进云端流水线。
- 本机的原生编译结果不得作为证据。本机只运行正式安装的产品，以及用签名运行时（`node.exe`、`python.exe`）跑脚本和 JS/TS 测试。
- 每个流水线的每个 job 都必须设 `timeout-minutes`，并在同一分支有新推送时取消旧运行。
- 本仓库公开。已提交的任何文件（含检查点、计划、WIP）不得含本机绝对路径、用户名、账户用量快照、令牌或私钥；路径用仓库相对路径或 `%USERPROFILE%`、`%LOCALAPPDATA%` 占位。由流水线机械检查，不靠自觉。
- 新增会被 gogoke 启动的子进程，引入时必须写明其在智能应用控制下的处境。发布不做代码签名，沿用现有更新链；该组件能否运行以 Owner 的 Windows 11（智能应用控制强制模式）上由正式安装包实际拉起的结果为准，被拦截时由 Owner 另行决定对策。主程序被放行不代表子进程会被放行；一个程序的放行不能外推到另一个程序或下一个版本。
- 任何人不得关闭、绕过或修改智能应用控制或其他系统安全设置。
- 发布不依赖代码签名证书：完整性由 Owner 离线签名的 `SHA256SUMS.windows` 保证，私钥不得进入仓库、构建树、CI 或发布资产。更新链的公钥、允许主机与协调器保持编译期常量，不得从数据或配置导入。

## 本机工作区卫生

任务开出的工作树、依赖目录、编译产物和临时副本，由开出它们的 agent 在任务结束时清理，不留给 Owner 或下一个 agent。确需保留的，在检查点写明理由。

## 事实账本

gogoke 的事实账本沿用 Git 与 GitHub，本机数据库只负责活跃协调。已接受的成果、事实、决定与证据以仓库和 PR 合并为准；数据库记录是工作状态，不是事实。完整决定见 [docs/governance/gogoke-ledger-decision.md](docs/governance/gogoke-ledger-decision.md)。
