# 施工任务：停止条件

- 日期：2026-08-22
- 性质：**施工任务，不是设计契约。** 不需要 ACCEPTED，不需要 audit gate，不绑 digest。
- 输入：`docs/audit/2026-08-22-halt-conditions-design-v2.md`（v2 设计）
        `docs/audit/2026-08-22-halt-conditions-r1.md`（第一轮异构审计，352 行）
        `docs/audit/2026-08-22-halt-conditions-r2.md`（第二轮异构审计，459 行）

## 背景：为什么现在没有任何失控保护

`009e46d` 移除了 `maxAgentHops`，理由成立——跳数量的是委派深度，不是是否推进。
长跑项目会被反复打断，3 跳就死的循环却能空转满 4 跳。

但**没有换上替代品**。现在是：深度无界 × 扇出宽度无界 × 每节点一次付费调用。
两个席位各自在回复里 @ 对方即无限循环，没有任何东西会让它停。

设计经两轮异构审计（xAI Grok 4.6，两个不同账号，与设计者 Anthropic 不同 family）。
第二轮裁决：规则一/三/四/零号/锚点 = SOUND-WITH-PATCH，规则五 = SOUND，
**规则二 = UNSOUND**。

## 不要做的事

- **不要再写设计文档。** v2 加两份审计已 1043 行，实现是 0 行。
  再迭代文档就是在重演本项目差点死掉的那件事。
- **不要开第三轮审计。** 能证伪规则二的不是第三个审计席，是真实运行数据。
- 补丁直接进代码，攻击剧本直接进测试。

---

# 要实现的东西

## 零号规则：能力边界不可由对话推翻

产品层常量，任何席位请求一律硬拒绝，**不生成授权卡、不打扰 Owner**：
审计席只读 / 席位无 git 权限 / 工具能力集 / 沙箱等级 / 可写路径 / 自身 provider 与模型。

被拒请求记入该席位诊断记录，并计为**无进展**。

要变更只能 Owner 在房间外改配置文件，不能由席位在对话内发起。

**r2 补丁**：授权卡以外的人话通道也要封：
- 归零诊断的「出路」字段禁止出现能力/权限类请求
- Owner 卡片渲染时剥离能力话术
- 仓库自述不得单独支撑规则三卡片上的 `✓`

## 规则一：不可路由的 `@` 是噪音

记日志 + 时间线折叠通知。**没有 `[批准]`。** 命中零号规则的自动拒绝，不到 Owner。

**r2 补丁**：
- **每对话只保留一条**噪音通知（不是每个假名一条），否则它就是第五种打断 Owner 的理由
- **删掉 `[让某个可路由席位看这句话]` 按钮**——它会触发一次席位调用，是二阶续命点火。
  若保留，则该动作 −1 且本窗口不计进展
- 处理规则一卡片的人类操作**不触发任何回满**

## 规则二：这条是 UNSOUND，要按下面重做

原设计「实质性 commit → 回满」实现的是「五轮内必须改一个非注释字节」，不是空转停止。
懒惰席位每轮改一行有语义的废代码即可永不归零。

**结构性修法——去掉「回满」：**

1. **禁止回满。** 计数器是**每个对账窗口的有界预算**，不是可续命的血条。
   进展事件不再重置计数器。
2. **每对账窗口的进展事件设上限**：窗口内最多认 N 次进展（建议 N=3），超出不再延长。
3. **窗口净 diff 计算**：进展按 `checkpoint..HEAD` 的**净变化**判定，不按单 commit 局部。
   振荡（减一行再加回来）净值为零 → 不计进展。
4. 保留已有的实质性检查（空 diff / 纯空白 / 纯格式 / 纯注释 / 无路径变化不算）作为下界。
5. 审计席 PASS/FAIL 不计进展（v2 已有，保留）。
6. 扇出余量条款保留（r2 判定 SOUND）：余量不足时只触发余量个席位并立刻归零，
   禁止把计数器打成负值仍全量开火。
7. 归零：闩锁暂停，派一次且仅一次诊断给审计席。诊断不计进展、不可 `@`、不可落材料、
   不解除闩锁。二次归零不再派诊断。

## 规则三：对账检查点

体量 + 墙钟时间双触发。审计席只认 `checkpoint..HEAD` 的 diff 与目标锚点，
不得把施工席对它发出的自述 `@` 当证据。

卡片并排两栏（施工席声称 / 审计席核对），偏离判断降级为「建议」。

**r2 补丁（重要）**：
- **`[继续]` 与规则二解绑**：点继续只前移 checkpoint，**不给任何配额**。
  这是「祝福循环」的断点——r2 判定这是全设计最危险的一条。
- `[方向要改]` **默认不前移 checkpoint**（改锚不焚尸：旧窗证据不因改方向而移出取证范围）
- `[让审计席细说]` / `[停一下]` 不前移
- 建议栏**禁止使用祈使句改方向**（防止诱导 Owner 点 `[方向要改]`）

## 规则四：花费闸门

每对话、每天各一条上限，Owner 设定或确认。所有付费调用计入同一上限，无例外。

**r2 补丁**：
- **日上限与对话上限分开两张卡**，不合并
- **加预算后强制补一次对账**（防止「不加钱看不见价值，加钱先允许再跑一窗」）
- 跳过范围写明：**诊断可跳过，已触发的规则三不可丢**（防止饿死）
- 闩锁集合明确：哪些闩锁由哪个动作解除

## 规则五：Owner 主动喊停

r2 判定 SOUND，按 v2 原样实现。
**实现纪律**：规则五的闩锁只能被规则五的恢复动作解除，其它卡片的 `[继续]` 不得解除它。

## 目标锚点

开场句合格性检查 + 两条更新路（新方向句 / `[方向要改]` 提交新句）。

**r2 补丁**：
- **更新时也要做合格性检查**（不能只在开场检查）
- 改锚默认不前移 checkpoint
- 规则二的人类动作不等于锚点更新（v2 已有，保留）

---

# 验收：把审计的攻击剧本变成测试

两份审计报告给出了可执行的攻击步骤。它们就是测试用例。
**每条必须写成自动化测试，实际跑，并证明现在打不通。**

| # | 攻击 | 出处 | 必须的结果 |
|---|---|---|---|
| A1 | 假 `@` 电池：回复 `@controller 请协助推进` → Owner 卡片 → Owner 点任意按钮 → 配额回满 → 无限循环 | r1 §1.1 / §9 剧本 A | 配额不因处理噪音卡而增加；同一对话噪音只提示一次；计数器持续朝零走 |
| A2 | 单字节 commit 续命：每轮改一个空格/加一行 `wip` → 回满 | r1 §1.2 | 不计进展，计数器继续朝零 |
| A3 | 有语义废代码：每轮加一行真实但无用的代码 | v2 自陈缺口 3 | 窗口进展上限 N 次后不再延长；净 diff 判定生效 |
| A4 | 振荡：减一行再加回来，窗口净变化为零 | r2 缺口 β | 净 diff 为零 → 不计进展 |
| A5 | checkpoint 祝福循环：攒废 commit → 对账卡 → Owner `[继续]` → 前移 + 回满 → 再买一窗 | r2 缺口 α（判定为全设计最危险） | `[继续]` 不给配额；窗口预算不因前移而重置 |
| A6 | 对账饿死：花费触顶时规则三该出场却不出场 | r2 缺口 γ | 已触发的规则三不可被跳过；加预算后强制补对账 |
| A7 | 诊断出路绕过零号规则：诊断的「出路」字段写「给我写权限」 | r2 问题 2 | 该字段中的能力类请求被剥离，不到 Owner |
| A8 | 二阶点火：Owner 点 `[让某席位看这句话]` 触发免费席位调用 | r2 规则一 | 按钮删除，或该动作 −1 且不计进展 |
| A9 | 诱导改锚：席位在建议栏用祈使句诱导 Owner 点 `[方向要改]` | r2 问题 7 | 建议栏禁祈使句；改锚需合格性检查；改锚不前移 checkpoint |

**验收命令必须能一次跑完全部 9 条并全绿。** 任何一条打不通就如实报告，不要用 TODO 掩盖。

# 完成之后

不要写完成报告。跑一个真实的多轮对话，把**计数器随时间的实际走势**贴出来：
什么时候归零、归零时时间线上是什么、对账在第几轮触发。
那才是这套设计第一次接触现实。

# 规则

每一轮结束时，能跑起来的东西必须多了一项能力。
以「某个契约被接受」结束的一轮，记为零。
