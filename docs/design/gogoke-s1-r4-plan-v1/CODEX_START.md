# Owner → Codex：S1-R4 一次性交接

Owner发送本文件与固定PLAN_COMMIT时，授权仅按S1-R4开始G0和六WP及新增认知基础任务的连续施工。旧停工授权不自动重用；本消息的人工投递就是新范围确认。GPT没有派发任务。

仓库taiyun668/gogo-party。PLAN_COMMIT以Owner给出的固定SHA为准；完整入口START_HERE_GPT_S1_R4_HANDOFF.md。产品输入88ef8e7dfbf5ba5aef58743dc45fa660f946276e；donor d6f291303ddc0c9a14f570266a4d9eff6d431593。不要用浮动branch替代，不恢复旧R3/AionCore/custom-journal指令。

先从固定commit读完整方案并运行verify_plan.py --root . --self-test（计划检查非产品PASS）。按RUNBOOK保全实际HEAD、dirty/untracked、未推送成果和活进程/custody；不能reset/clean或按名称kill。以PLAN_COMMIT另建codex/gogoke-s1-r4-execution-r1；如已存在先核实际状态不覆盖。旧PR#6/#7、方案分支及原冻结对象均不移动、不合并。

32任务和原19映射已由GPT完成，不再交施工者研究如何改线。按EXECUTION_PLAN、CHECKS、GATES_AUTHORIZATION、RUNBOOK先G0，依赖通过后连续G1—G5。工作依赖和原WP首验都有效；缺必需Windows/工具/许可就准确BLOCKED，同时做安全不依赖任务，不能将mock/旧CI/0测试记成PASS。

架构定界：单一T3-derived权威/SQLite/event/receipt；薄Rust层仅OS机制。开放driver/instance/model/seat分离；Antigravity承载Gemini、Pi走受管stdio。ContextFabric是本地事实/Manifest/暴露/lineage；DecisionRuntime真实受权选择但不授新权限；Outcome来自下游证据；Dream空闲快照/低优先/预算/留出/候选发布，不改原事实或生产代码。Jev不是第七客户端，不嵌入权重，不让confidence替代事实。真实后端默认关闭；测试命名空间不能污染prod资格。

按既有实际路由核Controller/worker/独立审查，worker不commit/push/自我验收；共享改动由Controller，容量不足排队，不增独立Codex会话绕限。普通编译、调试、测试和集中修复留Codex。只有改变内核/权威/隐私/原生身份/安全保证/产品能力或超授权时回GPT，不每WP往返。

许可仅限新分支源码/文档/测试、审生命周期后的固定源/锁依赖获取、隔离工具和临时根、fake模型/SDK/CLI、真实受控测试进程、当前用户私有IPC、Controller范围内commit/push及草稿证据PR。禁止真实账号/凭据/登录/费用/外发、用户数据Home迁移、remote/HTTP监听/live daemon/Tailscale/SSH/云、麦克风/model下载、全局安装/系统策略绕过、安装更新/registry/签名发布/合并。禁止弱化测试或产品能力。

每小批/3—5小项/目标约3分钟可保存工作落微恢复点；重大反证立即保存，耗时命令前intent后result。记录SHA/diff/dirty、plan/source/build/auth/fixture摘要、命令exit失败skip、进程custody和NEXT_ACTION。恢复先核已提交/在途/unknown，不盲重派。检查点不是审批点。

不要只写接包回执就停止：实际接包后开始G0，持续到完整阶段或真实阻断。最终回执含固定commit、全部修改、原33+新增26当期检查、68/157/84保全、失败修复、fresh初审/聚焦/最新全轴、未执行真实模型与平台轴、回滚/升级/residual custody。BLOCKED不是完成。阶段结束后S2研究回GPT。
