# S1-R4 v1 历史入口（施工排程已由修订稿替代）

当前计划入口：[S1-R4 修订版](../gogoke-s1-r4-plan-v2/PLAN.md)。

v1 原始完整字节保留在 `taiyun668/gogoke@e79dc718b98eef4a36ec88ee4d7ac43d63e0ca7a:docs/design/gogoke-s1-r4-plan-v1/`。本目录除本历史入口与 MANIFEST 标记外的文件保留作历史取证，不是与 v2 并行生效的施工指令。

旧 32 任务排程、59 due checks 与其他旧检查的去向见 v2 `PLAN.json`；原始未完成状态不改记 PASS，旧生产代码和测试不在本设计 PR 中删除。

当前仍受 MC-035 STOP 约束。计划合入后另开仅含新授权文件的 PR，由 Owner 本人合并；本文件、计划合入或计划自检均不恢复施工。
