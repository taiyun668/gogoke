# Web ChatGPT subagent channel: trial summary

This is trial evidence for PR #48, not adoption of `docs/model-routing.md` or acceptance of a web seat as an existing high-risk auditor. The route and GPT-6 Pro remain disabled for routine use. The initial requirement source was `main@5ec3d643a6015de34ef582b5d047e7cc7e46da43`; main later added the serial-dispatch and security-cooldown rules at `138c4c0547ac39996c8033011dce92d0b3891158`. The work used an isolated branch and did not change R2-06a.

| Trial | GitHub result | Observation |
| --- | --- | --- |
| WCS-01, independent document audit | `gpt/web-chatgpt-wcs01-audit@14a4854e007f96e2749a87dab1d76601747f5152` | One result file; parent is the reviewed main SHA. It identified routing and authority questions, not acceptance. |
| WCS-02, narrow test construction | `gpt/web-chatgpt-wcs02-tests@622b8f8db2763f19db46d642c6085d518130c296` | Only a test file and checkpoint changed. [Actions run 36209063759](https://github.com/taiyun668/gogoke/actions/runs/36209063759) on that exact head succeeded; raw logs show five executed tests, all `ok`. |
| WCS-03, expired-before-send fallback | No web result; local event and Codex subagent handoff | The deadline was already expired, no web Send occurred, and the subagent returned a read-only finding. This is a negative control, not a completed web task. |
| WCS-04, connector log access | `gpt/web-chatgpt-wcs04-actions@eea17043a8c84c8e49c293a4165fe9e6326fb205` | Web seat reported reading run metadata and individual raw log lines for [run 36208879407](https://github.com/taiyun668/gogoke/actions/runs/36208879407), including three executed test names. Codex independently checked the same run and log count. |
| WCS-05, fresh skill review | `gpt/web-chatgpt-wcs05-skill-audit@e827825dd3a5846e33c8ecfb8f904bf8f7d75f33` | Fixed-commit review found eight findings in the early ledger/skill candidate. This is not acceptance. The skill path and current CI were corrected after its reviewed SHA. |
| WCS-06, simulated security cooldown | No web result; local ledger state and Codex subagent handoff | A simulated 24-hour cooldown was written to the real local ledger. A fresh CLI process rejected a High reservation with exit 2 and directed work to Codex; a read-only Codex subagent checked the code path and returned findings. No web tab or Send was used. |
| WCS-07, first low-cost dispatch attempt | No web result | The former `prepare_dispatch.py` reserved Medium but its non-ASCII JSON output failed on the Windows console encoding. Luna released the unsent reservation. No browser call or Send occurred; the task ID was retired. That wrapper was later removed in favor of stepwise Luna browser use and windowless ledger calls. |
| WCS-08, second low-cost dispatch attempt | No web result | The single browser script call rejected the requested visible IAB tab in a subagent thread. No tab or Send was created. The script was changed to use a background IAB tab and still stop at any login or security page; WCS-09 is the replacement trial. |
| WCS-09, third low-cost dispatch attempt | No web result | A background IAB tab opened, but the script expected a legacy Chat radio control that the new blank-chat page did not expose. It refused before staging or Send and closed the tab. WCS-10 checks the actual blank-chat URL and zero message turns instead. |
| WCS-10, fourth low-cost dispatch attempt | No web result | The prompt was staged but the model-tier pill was absent in that immediate AX state. The script refused before Send and closed the tab. A separate read-only Luna diagnostic showed the pill can appear after the page settles, so WCS-11 waits for that visible control before the pre-send gate. The diagnostic tab was closed. |
| WCS-11, final scripted browser trial | `gpt/web-chatgpt-wcs11-single-dispatch@d78f0b797d11761fa861db944aa0ccba1d7bb7fb` | One Medium send was recorded. The result commit's sole parent is the required main SHA; only the assigned result file changed. The review found four direction-document ambiguities; these are trial findings, not PR acceptance. The reply's served model remains unverified. The background Luna IAB tab disappeared before the post-result read-only model check, so the workflow cannot claim that it closed the tab after verification; a later IAB inventory showed zero tabs. |
| WCS-12, manual Luna and no-window trial | `gpt/web-chatgpt-wcs12-manual-luna@c1005e018c0a2c08d2b4f23180774e8fc7dff8e9` | Luna selected GPT-5.6 Sol and Medium in the UI, clicked Send once, and saw new user and assistant turns. The real ledger has one sent reservation. The result commit's sole parent is the assigned candidate SHA; only its assigned result file changed. It reported two findings about failure propagation and test coverage; both were addressed after the reviewed SHA. The reply model remains unverified because the IAB tab was gone by result confirmation. |
| WCS-13, task-card lookup negative control | No web result | Luna searched the review base and not-yet-created result branch for the task card, so it stopped before reservation or Send. The card was actually committed to PR #48. The skill now names that trial card source and requires an immutable card SHA; the retired task ID was not reused. |
| WCS-14, pinned-card manual Luna trial | `gpt/web-chatgpt-wcs14-ledger-review@10b9b9af19a9f346208900d643e5d9c5959d4508` | The real ledger confirms one Medium send; Luna reported GPT-5.6 Sol and Medium before Send and new user/assistant turns afterward. The result commit has the assigned parent and only the assigned result file. It raised three accounting findings. The reply's actual model remains unverified because the subagent IAB tab was absent at result confirmation even though Luna marked it for handoff after Send. |
| WCS-15, foreground-boundary negative control | No web result | Luna stopped before reservation or navigation after treating the *possibility* of a login page as if one had appeared. No Send occurred. The skill now states that a zero-tab IAB is a normal starting point and that the foreground identity boundary applies when a sensitive page actually appears. WCS-15 was retired. |
| WCS-16, model-control negative control | No web result | Luna verified the immutable task card, reserved Medium, and opened a blank logged-in ChatGPT page. It observed a composer `中` control but did not expand it, so it could not prove the model family and stopped before Send. The unsent reservation was released and the tab closed; watcher did not start. Prior successful trials established that `中` opens `思考强度` and then `选择模型`, so the skill now documents that read-and-verify path. |
| WCS-17, persistent-draft negative control | No web result | The new ChatGPT page restored the WCS-16 unsent starter draft. Luna preserved it, released WCS-17's reservation, and stopped before Send; no watcher started. The next card explicitly names the one prior unsent draft that may be cleared after local `web_not_sent` evidence is checked. Other drafts still block Send. |
| WCS-18, subagent browser-availability negative control | No web result | A Luna agent initially misread the card and looked for scripts outside `.agents`; read-only reinspection corrected both facts. In its resumed turn, the Luna CUA session exposed no IAB browser, while the Controller's read-only IAB inventory showed one browser with zero tabs. Its reservation was released, no page opened, and no Send or watcher occurred. The task ID is retired. |
| WCS-19, unsent-proof negative control | No web result | Luna found the WCS-16 draft and stopped because ledger `status` did not itself prove WCS-16 was unsent. It released WCS-19's reservation and did not Send. A temporary `prove-unsent` command subsequently confirmed the local `web_not_sent` event, but Owner replaced that one-time recovery mechanism with a source rule for clearing Luna's own aborted input; the command was removed. |
| WCS-20, manual Luna retest after F3 repair | `gpt/web-chatgpt-wcs20-watcher-review@2c5b9c7adb048ccb491a6577729ddd99741f60ef` | Luna cleared the exact WCS-16 unsent draft, verified an empty composer, selected GPT-5.6 Sol / Medium, clicked Send once, and observed new user and assistant turns. The real ledger records one sent Medium reservation; the windowless watcher found the assigned GitHub result. Its commit has the assigned parent and changes only the result file. One watcher terminal-notification finding is addressed by an explicit deadline receipt check. The actual served reply model remains unverified because the subagent IAB tab was absent at result confirmation. |
| WCS-21, same-Luna pre-send cleanup control | No web Send or GitHub result | Luna staged the exact harmless WCS-21 text, verified it, intentionally stopped, cleared the composer and read it back empty, then released the Medium reservation and closed the tab. Independent local checks show `active_web_task=null`, no reservation or sent event for WCS-21, one `web_not_sent` trial event, and zero IAB ChatGPT tabs. |

The page-reported thinking times observed after completion were High **3m 13s**, Extra High **3m 17s**, Medium **59s**, and GPT-5.6 Sol Pro **7m 17s**. These are UI thinking indicators, not precise send-to-result wall times. The pre-send composer explicitly showed GPT-5.6 and the selected tier each time. After completion it still showed the same tier, with no visible downgrade or limit notice. The UI did **not** expose a distinct per-reply served-model field in these trials, so exact backend model identity remains unverified. No GPT-6 Pro message was sent.

The reply model was checked read-only after the trials. Under each reply, the `Retry / Switch model` menu offered `Retry with 5.6 Sol` for WCS-01, WCS-02, and WCS-04, and `Retry with 5.6 Pro` for WCS-05. Those controls describe a *future retry*, not the existing reply's served model. The adjacent reply controls and current composer tier also did not identify the served model. No Retry action was clicked. All four actual reply models remain `unverified` in the ledger; a visible mismatch in a future task will instead block the requested tier.

The first trial sequence **violated the subsequently added serial-dispatch rule**. Local reservation-to-completion intervals overlap for WCS-01/WCS-02 and WCS-02/WCS-04. The four completed ChatGPT tabs were then closed; the browser inventory showed zero remaining ChatGPT tabs. The revised skill checks the browser inventory before opening and before Send, holds one active task in the local ledger until GitHub result confirmation, and closes its tab immediately after confirmation. This tab rule is instruction-level; the ledger enforces the task slot atomically. A simulated expired cooldown remained blocked without manual clearance, and a new process read the persisted cooldown. The Codex fallback agent independently confirmed the local gate behavior and noted that Owner notification is an instruction/flag rather than an automated message, while an already running reply is not mechanically cancelled. No actual OS reboot, suspicious-activity warning, or security verification occurred.

The in-app browser session remained signed in across multiple new, saved Chat conversations during this trial. Its expiry time was not measured; a single continuous session only proves a lower bound. No credential, account-usage number, or conversation URL is in this repository. The local ledger and trial event log live under `%LOCALAPPDATA%\gogoke\web-chatgpt-subagent`.

The legacy `trial_simulation` cooldown was explicitly cleared from the real local ledger at Owner direction. A Controller `reconcile` then recorded the current baseline: only skill and web construction seat messages from ledger start are counted; prior and Owner manual ChatGPT use remain unknown. New or lost state blocks Send until that local reconcile, without asking Owner for usage history. Future cooldown demonstrations must use a temporary state directory. Real security cooldowns still require 24 hours and a Controller safe-page observation, with no automatic resumption.

WCS-05's three high-risk findings were repaired: switched replies require a switch flag and charge both requested and actual tiers; a limit with no reply has a terminal `limit_no_reply` record; a new or lost ledger fails closed until reconcile; and task IDs are unique across seats, including released reservations. Each defect has a Windows test. After WCS-11, Owner directed that Luna perform browser steps directly rather than run a browser script. The browser script and its prepare/finalize wrappers were removed. Ledger calls now use `pythonw.exe` with `CreateNoWindow` and a private receipt; the watcher runs under `pythonw.exe` and starts its `gh`/`codex queue` children with `CREATE_NO_WINDOW`. Four Windows tests cover those processes and the PowerShell wrappers. Process tests alone cannot prove that no brief unrelated desktop window appeared. This trial does not imply the route is enabled.

WCS-12 supplies that live observation: the required ledger receipts and watcher startup receipt all recorded `console_window_present=false`, the running watcher had no main-window handle, and Owner reported no visible window or black-console flash during dispatch. The browser was a background IAB tab, not a page visible on Owner's screen. The manual Luna agent marked the GitHub-card tab for handoff before navigating to ChatGPT; despite that mark, the ChatGPT tab was absent at result confirmation. The skill now explicitly marks the ChatGPT conversation **after** Send. This timing correction remains to be retested. The WCS-12 review's two findings led to a wrapper change that throws on nonzero ledger status and to tests of the actual PowerShell wrappers; the tests do not run a live `codex queue` child, while the WCS-12 watcher did reach GitHub and queued a notice without a reported window.

For WCS-12, local Codex `token_usage_record` entries between dispatch start and ledger result confirmation (651.6 seconds, 10m 51.6s) show **Controller:** 34 requests, 5,816,376 input tokens including 5,800,320 cached, 5,327 output tokens; **Luna:** 36 requests, 2,329,494 input tokens including 2,264,064 cached, 12,924 output tokens. Each counted response ID was unique. Controller usage includes implementation and review work performed while the watcher ran, so this interval is not an isolated dispatch-overhead measurement.

The WCS-14 review's F1 observation is an intentional conservative bound: a switched send counts both the requested attempt and the actual served tier, while the requested tier is blocked until observed reset because provider-side charging is not visible. F2's demand for a known historical usage quantity conflicts with Owner's explicit baseline rule: start at the Controller's reconcile time, mark earlier and manual use unknown, and use visible downgrade/limit notices to stop a tier. F3 exposed a real gap: deleting `state.json` also erased the task-ID idempotency set. An append-only private `task-ids.jsonl` now retires IDs before a reservation is committed, migrates known IDs from existing state, and blocks reuse after state loss and reconcile. A Windows regression test covers that sequence. The subagent IAB tab still did not survive to result confirmation. A brief active-turn waiter was implemented and tested locally, then removed when Owner prioritized low Controller request count: Luna now ends after page dispatch, and the background watcher notifies Controller later. Result-time model inspection and tab closing remain unverified when the tab disappears on Luna exit.

For the WCS-20 post-F3 retest, local per-response `token_usage_record` entries from dispatch through ledger result confirmation contain **Controller: 12 requests, 3,213,228 input tokens (3,206,016 cached), 2,868 output tokens; Luna: 35 requests, 2,690,727 input tokens (2,616,320 cached), 14,191 output tokens.** The Controller did no page operation. These are task-interval totals, not isolated API charges; the large cached Controller context remains a cost even with fewer waits.

WCS-18's IAB mismatch remains unexplained. Its first Luna CUA state was exactly `{"apps":[],"browsers":[{"id":"1","metadata":{"codexSessionId":"01a0dbe8-ed7d-7052-aba2-6ade6ca6b6cc"},"name":"Codex In-app Browser","type":"iab","tabs":[]}]}`. In a resumed turn, the same Luna session returned `{"apps":[],"browsers":[]}`, `listBrowsers()` returned `[]`, and `getBrowser({id:"iab"})` returned `Browser is not available: iab`; the Controller's read-only inventory at that time showed IAB id `1` with zero tabs. This suggests a subagent-turn browser binding problem, but the cause is not proven. Do not retry the same task ID or claim a foreground page from either inventory.

Owner replaced the WCS-16 one-time cleanup path with a source rule: the Luna that entered a new task draft must clear and visibly verify an empty composer before releasing the reservation or closing its tab when it abandons before Send. WCS-21 is the live negative control for that order. The temporary `prove-unsent` command and card-based clearance mechanism were removed; the WCS-16 draft had already been cleared once in WCS-20 after its local `web_not_sent` record was checked. No later task may treat that historical card field as general authorization to clear someone else's draft.

OpenAI's [Terms of Use](https://openai.com/policies/row-terms-of-use/) say users may not “Automatically or programmatically extract data or Output.” They also prohibit circumventing rate limits or restrictions. The workflow reads GitHub artifacts as its result and stops at local caps, but the applicability of the extraction clause to automated UI model checks and dispatch requires Owner judgment before enablement. The [ChatGPT model-limit article](https://help.openai.com/en/articles/20001354-gpt-56-and-gpt-6-pro-in-chatgpt) documents possible fallback and says reset time appears when available; the trial did not deliberately exhaust an allowance.

The trial did not establish the web seat's exact account-wide remaining allowance, other Chat seat consumption before ledger initialization, unpublished tier limits, or a verified provider reset time. These remain explicit limits on cost comparison and formal route adoption. The WCS-05 review's lower-priority findings on reset chronology and recovery remain separate from the repaired F1-F3 defects. The skill's path was moved to `.agents/skills` based on the [current Codex skill discovery documentation](https://learn.chatgpt.com/docs/build-skills); the older `.codex/skills` path in fixed trial cards describes the already-executed historical commits.

## Owner's later 6 Pro decision and WCS-22 preparation

The Owner's 2026-09-25 decision supersedes the preceding GPT-5.6 dispatch policy. The earlier WCS-01–WCS-21 rows remain historical trial evidence, not current targets. The route remains a draft and is limited to GPT-6 Pro with 25 **skill sends** per local day; the private ledger keeps old events for audit but exposes only the 6 Pro dispatch counter. The Owner explicitly authorized one 6 Pro trial and the local `enable-6-pro` entry cites that decision. It does not enable routine project routing.

WCS-20's measured fixed dispatch overhead was about $0.72, with Controller about $0.68 and Luna about $0.04. A web candidate must otherwise merit Astra and separately satisfy all five eligibility gates in the current skill. The private reservation records the original Astra seat and one-line economic reason. This estimate is specific to the observed thread interval; it is not a guarantee for another Controller context. A real task after handoff must repeat the cost measurement there.

The revised task contract sends exactly one message and permits no follow-up. A blocked or incomplete result goes to a Codex subagent with the result file; a timed-out task ID is void and a later GitHub commit is recorded only as a late result. The Controller verifies branch, scope, **main HEAD**, and a real CI run with executed tests before recording one-shot success. The public repo's main has no branch protection, so a task-card prohibition alone cannot enforce the GitHub write boundary. An unexpected main commit stops this route and is reported to Owner. The watcher uses the slow 6 Pro schedule. Neither the 25/day local cap nor a pre-send `6 Pro` label proves remaining account allowance or the reply's actual model; a visible limit or automatic downgrade blocks 6 Pro.

The current skill and Windows tests were revised for one tier, serial dispatch, persistent cooldown, unique task IDs, one-shot result outcomes, and late-result rejection. The live 6 Pro trial must still establish connector read/write, exact duration, actual-reply-model observation, memory banner status, CI evidence, and separate Controller/Luna token counts. No account-wide usage numbers or ChatGPT conversation URL belongs in this public record.

**WCS-22 did not enter the web channel.** The no-context Luna read its card and current skill but performed the card's audit itself using local `git show`, wrote the result file, and pushed `gpt/web-chatgpt-wcs22-pro-audit@d2ee1803d355aa2e426574f0561ab6d9fd997e48`. The local ledger has no WCS-22 reservation or Send, and Luna confirmed it never called IAB or `ledger.py`. Its GitHub commit therefore demonstrates ordinary Codex subagent work, **not** connector read/write, 6 Pro selection, model identity, or a web success; it was not approved in the web ledger. The card was ambiguous about Luna's courier role versus GPT's audit role. The skill now requires explicit `luna_action` and `web_starter_message`, forbids Luna from performing the web task, and keeps WCS-22's one-shot work in Codex instead of retrying that audit under another task ID.

The WCS-22 Codex audit observed that the ledger approved a `reply_model_unverified` result and counted it as one-shot success. The Owner's rules allow a task result with unknown served model while requiring the uncertainty to stay explicit; one-shot **task completion** must not claim model identity. The ledger now records `served_model_status` separately from task success, and its Windows test asserts that separation. The audit also noted that watcher tests do not exercise a live GitHub candidate lookup; the current result validation still relies on a live trial plus Controller inspection.

**WCS-23 stopped before Send; no 6 Pro live trial succeeded.** The first no-context Luna read the card and saw an empty IAB inventory but looked for wrappers under a nonexistent repository-root `scripts/` directory. It stopped without reservation. The Controller corrected the task card and skill to name the exact `.agents/skills/web-chatgpt-subagent/scripts/` paths. That Luna treated the no-follow-up rule as covering this pre-send correction and did not continue. A fresh no-context Luna on the **same task ID** read the corrected card, verified its GitHub SHA and main's pre-dispatch SHA, reserved the real ledger slot, then attempted `await cua.createBrowserTab("iab", "https://chatgpt.com/", {visible:true});`. CUA returned exactly `IAB visibility is not supported in a subagent thread`. No tab, draft, Send, ChatGPT reply, GitHub connector action, or watcher resulted. Luna released the unsent reservation; the durable ID journal retired WCS-23, and the private `web_not_sent` event records the Codex fallback. The task was not retried under a new number. The skill now states that subagent tab creation must use `{visible:false}`. This is a specific visibility-parameter error; WCS-18's separate browser-binding failure remains unexplained.

Raw browser states at the WCS-23 stop were: Luna `{"apps":[],"browsers":[{"id":"1","metadata":{"codexSessionId":"01a0dc3c-e04d-7bb3-aeeb-7bbcf5935377"},"name":"Codex In-app Browser","type":"iab","tabs":[]}]}`; Controller `{"apps":[],"browsers":[{"id":"1","metadata":{"codexSessionId":"01a0db4d-9881-7672-9c32-6ed2201d99c3"},"name":"Codex In-app Browser","type":"iab","tabs":[]}]}`. The latest Owner ambient context had one focused ChatGPT homepage tab, while both direct CUA inventories at dispatch time showed zero tabs; no claim is made that the background browser was visible to Owner. No suspicious-activity or verification page appeared because no ChatGPT tab opened.

Consequently the authorized 6 Pro trial did **not** verify GitHub connector read/write, send-to-result duration, per-reply served model, memory banner, or Controller/Luna token cost for a real web task. No 6 Pro message was charged. WCS-23's distinct governance audit was handed to a Codex Astra subagent without any web retry. The public record excludes account usage counts and conversation links. The Owner clarified that the original 6 Pro trial authorization **remains valid** because WCS-22 and WCS-23 stopped before Send; a new task ID can be used for the actual trial. Pre-Send task-card/path corrections are not prohibited follow-ups.

The Codex fallback's fixed-SHA findings and scope are in [WCS-23-CODEX-FALLBACK.md](WCS-23-CODEX-FALLBACK.md). It found an ambiguous Controller browser authority line and a reference to a foreground rule absent from the repository `AGENTS.md`; both are corrected in the draft skill. That fallback is separate from the web trial and is not counted as one-shot web success. Required hidden-process receipts for the WCS-23 reservation/release reported no console window, and no IAB tab opened; this does not independently prove that the Owner's desktop had no unrelated brief window, so a full no-popup live web run remains unverified.

The public source hygiene run on an intermediate WCS-23 card [failed](https://github.com/taiyun668/gogoke/actions/runs/36221591334) because the card included two local user-profile paths. Those fields were removed from the current tree; Luna now resolves the wrappers from repository-relative paths at runtime. The intermediate public commit existed and remains a historical exposure even though the current tree is corrected.

At corrected code/card commit `5795e59c610d0fd2796533dd8922ba120e761f93`, [Windows skill run 36221913385](https://github.com/taiyun668/gogoke/actions/runs/36221913385) passed with **13 executed tests** in the raw log, and [public source hygiene run 36221913387](https://github.com/taiyun668/gogoke/actions/runs/36221913387) passed with **zero current-tree leaks**. Both are real cloud runs on that exact SHA. The latter does not erase the intermediate commit's historical exposure. The web trial itself still has no Send or GitHub connector result, so these checks do not substitute for a successful 6 Pro run.

## Script-to-manual Luna pitfall check

The current skill was checked against each WCS-01–WCS-21 observation before another 6 Pro dispatch:

| Trial(s) | Operational lesson | Current skill coverage |
| --- | --- | --- |
| 01–05 | Exact pre-Send tier; a future Retry choice does not identify the served reply model; commit and CI are the result evidence. | Composer `6 Pro` gate, read-only served-model check, GitHub/real executed-test verification. |
| 01/02/04, 06 | Early overlapping sends; persistent security cooldown and Codex fallback. | One active ledger task and one ChatGPT tab; 24-hour manual-clear cooldown. |
| 07 | Console encoding failure in the removed preparation script. | No preparation/browser script; hidden `pythonw` ledger receipts checked for failure and console presence. |
| 08 | Visible IAB tab rejected in subagent thread. | Explicit background `createBrowserTab(...,{visible:false})`; never `visible:true`. |
| 09 | New blank chat lacks the old Chat radio control. | Check actual new-chat URL and zero message turns instead. |
| 10 | Model pill may be absent from the first accessibility snapshot. | Let page settle, reread, then verify stable pill before Send. |
| 11–12, 14 | GitHub-card tab handoff did not preserve ChatGPT conversation; even a conversation handoff may vanish. | Mark conversation tab after Send; if gone, served model and explicit close stay unverified. |
| 13 | Card was searched at review SHA/result branch rather than its own branch. | Read card at verified immutable commit on card branch. |
| 15 | Hypothetical login page was mistaken for an actual login block. | Zero-tab IAB may be opened; stop only if the displayed page is sensitive. |
| 16 | `中` did not itself prove model family. | Expand the pill, inspect nested model/strength controls, require resolved `6 Pro` label. |
| 17, 19, 21 | Leftover drafts and premature release; one-time `prove-unsent` path removed. | Preserve unrelated draft; same Luna clears its own text and confirms empty before release/close. |
| 18 | Subagent IAB availability differed from Controller. | Record both raw states and stop without changing task ID to evade the failure. |
| 20 | Watcher terminal notice could fail silently. | Controller inspects one watcher receipt at deadline for candidate/error/expiry. |

The removed 20-test suite also covered a sent or uncertain reservation refusing `release`; the 13-test revision lost that check. A Windows CLI test now exercises both states against the real `ledger.py` and checks that the slot remains held and task ID cannot be reused. The intermediate public-path exposure is accepted by Owner as historical; no history rewrite is planned. Every future task card must pass `tools/check-public-source.py` **before** its commit.

## Owner direct-operation correction and WCS-24 stop

Owner superseded the Luna courier route: the Controller now opens and operates one **foreground** IAB ChatGPT tab, sends once, then checks the page read-only about every five minutes while the hidden watcher observes GitHub. WCS-24 had already been assigned to Luna when this change arrived. Luna reported **no Send**, but it had staged text and reserved the real ledger slot. Its CUA binding then failed and returned `{"apps":[],"browsers":[]}`, so it could not prove its original draft was cleared. The Controller's IAB inventory was empty. The Controller opened one foreground ChatGPT new chat, observed an empty composer, explicitly set/read it empty, released the unsent reservation with a windowless ledger receipt, and closed the tab; the final IAB inventory had zero tabs. This verifies the Controller's new-chat composer only; **the original Luna draft clearance remains unverified**. The private event records `web_not_sent`, the released reservation, and that limit. WCS-24's task ID is retired, with no Pro message or connector result.

The one-Send Owner authorization remains available. The direct-operation skill now includes the project construction discipline and inline starter/task-card templates. Ordinary GitHub write retries while GPT is still generating are normal; only stopped generation without later actions is an interruption. Connector write-confirmation prompts are left untouched and reported to Owner. The next distinct task uses a fresh ID and a task card scanned **before commit**. The Controller will report Send-phase and page-patrol request/input/cached/output tokens separately, plus Send-to-result-commit wall time, if a real Send and GitHub result occur.

## WCS-25 direct Controller trial: Owner-confirmed pre-Send failure

The task card `WCS-25.json` was checked with the scanner's positive control and `--file` mode **before commit** (`leaks=0`), then pinned at `codex/web-chatgpt-subagent-channel@9f382a2f547821bec3e5d0fb50c77e34410c4fba`. It assigned an independent scanner audit of fixed commit `8b04a707b2d9f3b4163193aaa796258cb17bb946` to `gpt/web-chatgpt-wcs25-source-scan-audit`, result file only. A separate [rule crosswalk](WCS-25-RULE-CROSSWALK.md) maps every Owner requirement to the direct-operation skill and its inline templates.

The real ledger had no active task or cooldown; Controller's live IAB inventory had zero tabs and its latest request input was below the 272,000-token threshold. Controller reserved the Pro slot with an Astra economic reason and opened **one foreground** ChatGPT tab. The new chat had zero turns. After the page settled, the thinking-strength menu reported `Pro, item 5 of 5`, and the resolved composer label was `6 Pro`. The one starter message included the required governance sections, excluded the long-window sections, and stated the remote-write recovery rules directly. No limit, downgrade, security, login, connector-confirmation, or memory-used notice was observed before the click.

Controller attempted the accessibility control labelled `发送提示词` **once**. The tool then showed an “Add to Project: Gogoke” suggestion; the composer retained the entire starter and the URL stayed at new chat. A read-only check after ten seconds still had **no new user or assistant turn**. At first the ledger conservatively recorded `mark-uncertain` and Codex fallback. Owner then confirmed the stronger page rule: a successful Send always navigates to a conversation content page and creates a new user turn; remaining at new chat means **not sent**. Controller reopened the **same** tab, found the original WCS-25 draft still staged at the new-chat URL, set the composer empty, read back an empty composer, and closed the tab. The tested hidden `correct-unsent` transaction changed the local event to `web_not_sent`, restored local capacity, excluded it from the one-shot success-rate denominator, and **kept WCS-25's task ID retired**. No watcher was started, no assigned result branch appeared, and remote `main` remained `138c4c0547ac39996c8033011dce92d0b3891158`. Actual reply model, GitHub connector read/write, page patrol during generation, and Send-to-result-commit duration were **not measured**; the Owner's 6 Pro Send-trial authorization remains valid.

The screenshot and accessibility tree identify the real Send control as the blue up-arrow **inside the composer's lower-right corner**, with `ID: composer-submit-button`. “Add to Project: Gogoke” is a **separate suggestion button below the composer**. The WCS-25 `click(index)` return did not report its low-level hit target. Its observed effects were an unchanged new-chat URL, retained composer/focus, and the newly displayed project suggestion; it did **not** activate a successful Send. A harmless `Gogoke` diagnostic draft showed the real Send button and no suggestion, then was cleared and read back empty. These observations do not prove exactly which node swallowed the prior click; a changing accessibility index or suggestion/focus transition remains an inference. The revised skill requires a fresh AX state **and screenshot** before locating the button, or one Return on the focused composer as the single Send action if button targeting is ambiguous. Navigation plus a new user turn is the required success test; a new-chat URL is a pre-Send failure, never `mark-uncertain`.

Local per-response records from reservation to the **initial** `mark-uncertain`: **Controller Send-attempt phase: 18 requests, 4,111,471 input tokens (4,087,936 cached), 4,223 output tokens.** From that mark to the initial ledger fallback finish: **read-only investigation: 4 requests, 972,774 input tokens (969,600 cached), 2,225 output tokens.** These historical interval figures precede Owner's correction; they do not imply a real Send. There was **no confirmed-Send patrol phase** and no result commit, so no actual direct-route per-task cost or end-to-end duration can be inferred. The required ledger calls used the hidden wrapper with `console_window_present=false`; no separate visible process was observed through these receipts, but Owner-screen popup absence was not independently confirmed for this attempt.

The scanner audit was handed to a Codex Astra subagent and independently checked; its [five fixed-SHA findings](WCS-25-CODEX-FALLBACK.md) are Codex fallback evidence only. They are not a web answer or connector result. No scanner implementation change was made in this PR.

The direct-route code and trial-record commit `825d5350774be03c24efbacf153e6cf7bb183901` passed [Windows skill run 36223544113](https://github.com/taiyun668/gogoke/actions/runs/36223544113) with **14 actually executed tests** in raw logs, including the restored sent/uncertain release prohibition. [Public source hygiene run 36223544048](https://github.com/taiyun668/gogoke/actions/runs/36223544048) on that exact commit scanned **24,454 current-tree files with zero leaks**. These cloud checks verify the committed skill/ledger tree, not a successful GPT-6 Pro Send or GitHub connector write.

## WCS-26 direct Controller 6 Pro trial: GitHub result accepted

Owner retained the original one-Send trial authorization after WCS-25's confirmed pre-Send failure and later removed the 272,000-token hard dispatch gate. `WCS-26.json` passed the public source check before commit and was pinned at `codex/web-chatgpt-subagent-channel@f12f3cf1445dc1dd800893e40e09aedec2617875`. It assigned an independent static audit of fixed SHA `ef01e5e35f80e6bdffd4ef0eb657652522ac388d` to `gpt/web-chatgpt-wcs26-process-audit`, with only `WCS-26-RESULT.md` writable. Before dispatch, the real ledger showed a reconciled baseline, Pro enabled, no cooldown/active task, and local capacity; remote `main` was `138c4c0547ac39996c8033011dce92d0b3891158`. The Controller reserved once with an Astra economic reason. The only IAB tab was a foreground new chat with zero prior turns, visible `6 Pro`, and no visible limit, downgrade, verification, or memory-used notice. The required single starter was staged and reread. A **single Return** on the focused composer navigated to a conversation content URL, created a new user turn, and showed `Pro 思考中`; the hidden ledger recorded Send at **2026-09-26 06:49:01.918699Z**. This is a confirmed Send, unlike WCS-25.

The result commit [bea07715d09339494c5784948031b8fe1ecd244c](https://github.com/taiyun668/gogoke/commit/bea07715d09339494c5784948031b8fe1ecd244c) was created at **06:54:37Z**, **5 minutes 35 seconds after Send**. The background watcher first detected it at 06:55:18Z. Controller independently checked that the assigned branch head equals that SHA; it has parent exactly the assigned review/base SHA and adds only the allowed result file. The file names the fixed review SHA, reports four conditional static findings with file/line evidence and falsifying tests, and correctly labels GPT's own executed-test count zero/NOT_RUN. Remote `main` still equalled the pre-dispatch SHA. Controller dispatched [native-host CI run 36225472701](https://github.com/taiyun668/gogoke/actions/runs/36225472701) **on that exact result SHA**; both jobs completed successfully. Raw logs show **383 native-host library tests passed** and the server job's machine result reports **536 executed/passed, zero failed/skipped**. The result therefore passed the task's GitHub scope and real nonzero CI acceptance gate. The ledger records one-shot task success and one charged Send. The page's pre-Send `6 Pro` label and `Pro 思考中` do not prove the reply's actual served model; no unambiguous served-model field was found, so the ledger records **`served_model_status=unverified`**. The IAB tab was closed after verification and the inventory was empty. Ledger/watcher process receipts showed no console window; absence of other Owner-screen popups was not independently observed.

Controller token accounting below uses the session's per-response `last_token_usage` records, with disjoint ordinal ranges **7800–7973** (dispatch/Send), **7980–8127** (post-Send patrol/wait), and **8136–8376** (GitHub/CI verification and the wakeup-method repair in progress). These ranges do not include task-card preparation before the dispatch range or later documentation work. The patrol range includes **nine `sleep` calls**, each a full Controller request; the watcher had detected the result while the Controller turn remained active, delaying action until the next patrol. Owner corrected that design. The new skill ends the Controller turn after Send and after each read-only patrol; the watcher queues five-minute patrol reminders and immediately queues a result notice, repeating the latter every 30 seconds until the Controller acknowledges the exact candidate SHA. The 30-second repeated-wake mechanism still requires a live wakeup probe; unit coverage alone is not counted as that probe.

| Controller phase | Requests | Input tokens | Cached input | Output tokens |
| --- | ---: | ---: | ---: | ---: |
| Dispatch and confirmed Send | 26 | 1,843,799 | 1,809,920 | 3,514 |
| Patrol and active-turn waiting | 23 | 2,053,003 | 2,041,472 | 3,394 |
| Result/CI verification plus wakeup-method repair | 32 | 3,922,136 | 3,868,416 | 14,141 |
